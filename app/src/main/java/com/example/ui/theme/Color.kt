package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// White canvas with soft green accents
val EmergencyBackground = Color(0xFFF8FAF9) // Clean crisp off-white canvas
val EmergencySurface = Color(0xFFFFFFFF)    // Pure white cards & elevated surfaces
val EmergencySurfaceVariant = Color(0xFFE8F5E9) // Soft soothing pale green container
val EmergencySurfaceBorder = Color(0xFFE2E8F0) // Subtle neutral border

// Bright Red for Emergency Warnings & SOS (MUST be BRIGHT RED)
val EmergencyRed = Color(0xFFFF1744)       // Vibrant bright primary red
val EmergencyRedDark = Color(0xFFD50000)   // Deep bold red
val EmergencyRedGlow = Color(0xFFFF0033)   // Intense radiant red beacon
val EmergencyRedLight = Color(0xFFFFEBEE)  // Very soft red badge container

// Soft Green Accents
val SignalGreen = Color(0xFF10B981)        // Balanced natural green
val SignalGreenLight = Color(0xFF059669)   // Readable green on light backgrounds
val SignalGreenSoft = Color(0xFFD1FAE5)    // Soft mint green container
val SignalGreenAccent = Color(0xFF34D399)  // Vibrant soft mint

// Amber / Blue helpers
val WarningAmber = Color(0xFFD97706)       // High-contrast amber for light bg
val InfoBlue = Color(0xFF2563EB)

// Crisp legible typography on white backgrounds
val TextPrimary = Color(0xFF0F172A)        // Deep slate almost-black text
val TextSecondary = Color(0xFF475569)      // Balanced readable slate
val TextMuted = Color(0xFF64748B)          // Muted slate

val CardBorder = Color(0xFFE2E8F0)         // Clean light divider

